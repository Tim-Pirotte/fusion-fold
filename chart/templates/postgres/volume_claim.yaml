apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-postgres
spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: postgres-volume
  resources:
    requests:
      storage: {{ .Values.postgres.pvc.storage }}
