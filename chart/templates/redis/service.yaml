apiVersion: v1
kind: Service
metadata:
  name: svc-redis

spec:
  selector:
    app: redis

  ports:
    - port: {{ .Values.redis.service.port }}
      targetPort: {{ .Values.redis.deployment.containerPort }}
