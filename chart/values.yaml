imagePullSecret: ghcr-secret

frontend:
  deployment:
    repository: ghcr.io/tim-pirotte/frontend
    tag: latest
    pullPolicy: Always
    containerPort: 8000

  service:
    type: NodePort
    port: 8000
    targetPort: 8000
    nodePort: 30080
